#!/usr/bin/python

import commands

def get_password(account=None, server=None):
  params = {
    'account': account,
    'server': server
  }
  cmd = "security find-internet-password -w -a %(account)s -s %(server)s" % params
  (status, output) = commands.getstatusoutput(cmd)
  return output
